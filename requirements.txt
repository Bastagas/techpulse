# TechPulse — dépendances Python consolidées
# Utilisé par le path MAMP (sans Docker) :
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Le path Docker utilise scraper/pyproject.toml et api/pyproject.toml

# ─── Scraper ────────────────────────────────────
httpx>=0.27
hishel>=0.0.30
tenacity>=9.0
selectolax>=0.3.24
beautifulsoup4>=4.12
lxml>=5.2
sqlalchemy>=2.0.30
pymysql>=1.1
cryptography>=42.0
pydantic>=2.7
pydantic-settings>=2.3
loguru>=0.7
python-dotenv>=1.0
spacy>=3.7
click>=8.1

# ─── API Flask ──────────────────────────────────
flask>=3.0
flask-smorest>=0.44
flask-cors>=4.0
marshmallow>=3.21
apscheduler>=3.10
gunicorn>=22.0

# ─── Dev (optionnel) ────────────────────────────
pytest>=8.2
pytest-asyncio>=0.23
respx>=0.21
ruff>=0.5
